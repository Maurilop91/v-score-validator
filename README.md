# v-score-validator

## Prompt Log

A running record of every prompt given in this project.

1. `/init` — Analyze this codebase and create a CLAUDE.md file with common commands and high-level architecture.
2. `git add .` / `git commit -m "chore: initialize v-score validator project"`
3. add a readme file and fill that file with every prompt i will give you include this
4. commit this
5. Read SPEC.md carefully. We are building an agentic V-Score Validator (must not be a manual calculator). Defined PoC evaluators (Technical Novelty, Defined Scope, Resource Accessibility, Measurable Outcome) and Market evaluators (Pain Severity, Willingness to Pay, Market Size, Differentiation); required architecture of one orchestrator agent, eight evaluator agents, one skill per criterion, one weighted-score skill, one verdict skill; per-evaluator and orchestrator responsibilities; "Do not create or modify files yet." — produce a detailed implementation plan (folder structure, agent/skill responsibilities, shared schema, orchestration sequence, error handling, verification, risks) and wait for approval before editing files.
6. remember to fill the prompts in readme file
7. Implement only the skill layer from the approved architecture plan (read SPEC.md first, do not modify it). Create ten skills: the eight criterion evaluators plus calculate-v-score and generate-verdict. Each criterion SKILL.md needs valid YAML frontmatter, purpose, evaluation dimensions, positive/negative signals, missing-information rules, scoring anchors (1-3/4-6/7-8/9-10), evidence-handling rules, anti-hallucination rules, the common evaluator output contract, and one scoring example. calculate-v-score implements PoC = TN×3 + DS×4 + RA×2 + MO×1 and Market = PS×4 + WTP×3 + MS×2 + Diff×1; generate-verdict uses the 65-point threshold to map to Go / De-risk First / Validate Demand / Reframe or Shelve. Constraints: no agents, no UI, no external research, non-overlapping scopes, no buzzword rewards, missing info lowers confidence, keep technical vs product novelty separate. Then verify frontmatter validity, shared contract, and overlaps.
8. Review the ten skills (no edits): trigger precision, accidental cross-triggering, anchor distinctness, evidence vs assumptions, missing-information penalties, shared-contract adherence, buzzword rewards, and duplicate reasoning. Return issues with severity, affected file, and recommended fix.
9. Apply only the high- and medium-severity fixes from the review (do not change formulas, add agents, expand scope, or break the shared contract). Then rerun the review and confirm no high/medium findings remain.
10. git add . / git commit -m "feat: add v-score evaluation skills"
11. Implement only the eight specialist evaluator agents (technical-novelty, defined-scope, resource-accessibility, measurable-outcome, pain-severity, willingness-to-pay, market-size, differentiation). Each agent: valid frontmatter, precise description, evaluates exactly one criterion, uses exactly one corresponding skill, receives the complete original idea, extracts direct evidence before scoring, identifies missing information, scores 1-10, returns the common JSON contract, never computes weighted/final scores, never evaluates other criteria, never invents facts, distinguishes facts from inference, lowers confidence on weak evidence, and returns JSON only. Each file includes Role, Single responsibility, Non-responsibilities, Required skill, Workflow, Guardrails, Output schema, and Retry behavior. Do not create the orchestrator/UI/database. Then produce a responsibility matrix.
12. Test only the technical-novelty-agent with three ideas (A: to-do list app; B: "An AI platform that transforms healthcare."; C: privacy-preserving collaborative fraud detection with banks training a shared model without exchanging raw data). Verify score 1-10, reasoning only about technical novelty, buzzwords not rewarded alone, missing tech info reduces confidence, no market reasoning, exact JSON contract. Do not edit files. Return each result, pass/fail per condition, issues, and recommended fixes.
13. Run a focused validation of all eight specialist agents using the AI-powered restaurant ingredient-demand-forecasting idea (10 restaurants in Montevideo, 8 weeks, reduce food waste >=15%, manual estimation today, intended subscription but no price interviews, existing inventory tools forecast but product focuses on small local restaurants and simple onboarding). Invoke every evaluator independently; check single-criterion focus, evidence use, missing-info, score 1-10, reasoning supports score, shared contract, no duplicated responsibility. Do not calculate PoC/Market or create/edit the orchestrator. Return a table (criterion, score, confidence, strongest evidence, largest gap, pass/fail).
14. remember to always fill the readme with all the prompts
15. Implement the V-Score orchestrator agent (read SPEC.md, the eight specialist agents, calculate-v-score, and generate-verdict). Workflow: accept one idea, reject empty input, preserve original wording, invoke all eight evaluators (parallel when supported), collect responses, validate (exactly eight results, each criterion once, integer scores 1-10, reasoning/confidence/missingInformation present), retry one invalid response once, hard-stop on persistent failure, run calculate-v-score only when all valid, run generate-verdict, and preserve traceability from each weighted score to its specialist output. Final response follows the given orchestrator JSON schema (idea, evaluations, scores with calculation strings, verdict, strongestSignals, largestRisks, missingInformation, recommendedNextExperiment). Restrictions: no silent score changes, no calculating with missing evaluators, no invented evidence, no web, no UI, no hiding disagreement, no numbers-only output, no verdict that contradicts specialist reasoning. Then explain the orchestration sequence, invalid-output handling, where each score enters the formula, and confirm 65 is treated as High.
16. Test calculate-v-score and generate-verdict deterministically (independent of LLM behavior) with five cases: all 10s -> 100/100/Go; all 1s -> 10/10/Reframe or Shelve; PoC 5s + Market 10s -> 50/100/De-risk First; PoC 10s + Market 5s -> 100/50/Validate Demand; values giving exactly 65/65 -> both High/Go. Substitute values, show arithmetic, compare actual vs expected, pass/fail; fix only the calc or verdict skill if any case fails, then rerun all five.
17. Run an end-to-end evaluation with the V-Score orchestrator on the Montevideo restaurant forecasting idea (no file changes on first run). Validate all eight ran, each criterion once, scores 1-10, evidence-based reasoning, WTP reflects missing pricing validation, Measurable Outcome reflects 8-week/15% target, Defined Scope reflects ten-restaurant Montevideo MVP, Differentiation reflects existing forecasting products, correct weighted formulas, verdict matches the matrix, a concrete next experiment is present, and no invented facts. Return full orchestrator output, verification checklist, inconsistencies, pass/fail; if a failure, locate it (agent/skill/orchestrator), apply the smallest fix, and rerun.
18. git add . / git commit -m "feat: add v-score agent orchestration"
19. Create a deterministic slash command /validate-idea that starts the V-Score workflow via the existing orchestrator: ask for the idea if none provided, pass the original idea to the orchestrator, wait for all eight evaluations, and return criterion breakdown, PoC score, Market score, verdict, largest risks, missing information, and recommended next experiment. Must not duplicate scoring logic, manually call criterion skills, replace the orchestrator, add criteria, browse the web, or alter scores. Add a short usage example.
20. Add a minimal persistent memory layer for completed evaluations (only after the core workflow works): evaluations/scores.json (append one entry per successful evaluation, per the given schema) and evaluations/lessons.md (purpose only, no automatic rules yet). Before each evaluation read scores.json, find the most similar prior idea by simple tag/keyword overlap, include it only as labeled reference, never copy prior scores. Restrictions: no database, no embeddings, no external services, historical scores never override current evidence, never save failed/incomplete evaluations. Verify: one successful eval grows scores.json by one; a related idea recalls the previous entry; new scores are independently generated.
21. Add a minimal human-approved self-learning loop: feedback on a completed evaluation -> the system proposes ONE reusable rule appended to evaluations/lessons.md with Status: proposed -> a human may set Status: accepted -> only accepted rules (scoped to their named criterion) load before future evaluations. Use the given lesson schema (Lesson ID, Status, Criterion, Observed issue, Evidence, Proposed rule, Expected effect). Create a /capture-v-score-lesson command asking for evaluation ID, criterion, observed problem, expected correction. Restrictions: never auto-accept, never change formulas, never make a rule without evidence, never cross criteria, never delete lessons, never modify a criterion skill directly, accepted lessons are runtime context only. Demo: score a vague "AI-powered" idea, feedback that Technical Novelty was too high, propose+accept the LLM-API cap rule, re-evaluate a similar idea, show the accepted rule appears in evaluator context and the score still depends on new evidence.
22. [Idea evaluation] AI-powered restaurant forecasting idea (Montevideo, ten restaurants 1-3 locations, 8-week MVP, POS-export integration, waste -15%/shortages -10%/usage 8-of-10, $60/location subscription with 3 owners at $40-80). Full orchestrator run -> PoC 63 / Market 56 / Reframe or Shelve; persisted eval-2026-07-31-003.
23. [Idea evaluation, duplicate] Same Montevideo idea resubmitted -> recognized as identical to eval-003; ran a 3x consistency check (chosen via question). Result: 6/8 criteria stable, TN (3-4) and RA (6-7) vary, PoC ranged 63-66 and the verdict flipped once to Validate Demand at PoC 66 -> flagged threshold instability; not persisted.
24. [Idea evaluation] Federated-learning cross-bank fraud-detection network (LatAm regional banks <500k customers, privacy-preserving aggregation, 3-bank/6-month pilot, +10% fraud/FP-below-baseline/no-raw-data-leaves, 2 banks spend >$100k/yr, no bank committed). Full run -> PoC 56 / Market 64 / Reframe or Shelve; persisted eval-2026-07-31-004.
25. Update only the final output formatting of the V-Score orchestrator: keep the full analysis intact as PART 1, then a large separator, then a concise PART 2 "Final Result" (PoC/Market scores, official verdict, Viability Status via fixed mapping, plain-language Final Decision, Why paragraph, concrete Recommended Next Step). Do not change any score, formula, the 65 threshold, verdict names, or memory/lessons behavior. Test on the fraud-detection evaluation (PoC 56 / Market 64 / Reframe or Shelve -> NOT VIABLE YET).
